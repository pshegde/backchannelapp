<%= form_for(@login) do |f| %>
  <% if @login.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@login.errors.count, "error") %> prohibited this login from being saved:</h2>

      <ul>
      <% @login.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :username %><br />
      <%= f.text_field :username %>
    </div>

    <div class="field">
      <%= f.label :original_password %><br />
      <%= f.text_field :original_password %>
    </div>

    <div class="actions">
      <%= f.submit "login"  %>
    </div>
    <div class="actions">
      <%= link_to "create new user" ,:controller=>'users',:action=>'new' %>
    </div>
<% end %>
